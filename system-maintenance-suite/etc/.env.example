# Sources to back up (bash array)
BACKUP_SOURCES=("/etc" "$HOME/Documents")

# Destination directory for snapshots
BACKUP_DEST="$HOME/backups"

# Keep the N most recent snapshots
BACKUP_RETENTION=7

# Log scanning defaults
LOG_SINCE="1h"          # e.g., "1h", "today"
LOG_PRIORITY="warning"  # emerg|alert|crit|err|warning|notice|info|debug

# Force a package manager (apt-get|dnf|pacman|zypper) if auto-detect is wrong
PM_OVERRIDE=""
